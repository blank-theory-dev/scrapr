__pycache__/
*.pyc
.venv/
.DS_Store

# Per-run checkpoints — scraped data, not source
.scrapr_runs/

# Scraped output, catalogue dumps and client input lists. This repo is PUBLIC, so
# supplier pricing and part lists must not be committed. Keep them local.
results.csv
*_export.csv
Machter*.csv
all_skus.txt
products_sample.json
